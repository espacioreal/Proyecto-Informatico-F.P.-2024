// Funciones generales para la aplicación

// Al cargar la página
document.addEventListener('DOMContentLoaded', function() {
    console.log('Página cargada');
    // Aquí puedes añadir más funcionalidades que se ejecuten al cargar la página
});

// Ejemplo: Función para mostrar un mensaje de alerta
function showAlert(message) {
    alert(message);
}
